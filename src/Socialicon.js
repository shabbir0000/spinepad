import React from 'react';


const Socialicon= ({Icons}) => {
  return (
    <div>
      icons
        {/* {
            Icons.map(icons=>(
            <span key={icons.name}>
              <ion-icons name={icons.name}></ion-icons>
            </span> 
            ))} */}
    </div>
  )
}

export default Socialicon;